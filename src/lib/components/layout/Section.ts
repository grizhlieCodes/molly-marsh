
/**
 * Type alias for ARIA-labelledby attribute.
 * This represents the ID of a heading or any element used to label a section.
 * Typically used for accessibility to associate a section with its label.
 * @type {string}
 */
export type SectionLabeledBy = string;

/**
 * Type alias for the ID of a section.
 * This represents a unique identifier for a section in a document.
 * Typically used to reference or navigate to specific sections.
 * @type {string}
 */

export type SectionID = string;